const idadeMin = 18;
const idadePessoa = 20;

/*
if (idadePessoa >= idadeMin) {
    console.log("Pode entrar!");
} else {
    console.log("Não pode entrar!");
}
*/

// Agora utilizando Operador ternário (condição ? se verdadeiro : se falso)

console.log(idadePessoa >= idadeMin ? "Pode entrar!" : "Não pode entrar!");