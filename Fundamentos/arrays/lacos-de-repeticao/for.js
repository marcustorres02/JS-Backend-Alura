const numeros = [100, 200, 300, 400, 500, 600];

// For possui três partes: 1. Inicialização; 2. Condição de parada; 3. Incremento

// Primeira expressão é executada apenas uma vez
// Segunda expressão é a condição de execução
// Terceira expressão é executada sempre ao final de um laço

for (let indice = 0; indice < numeros.length; indice++) {
    console.log(numeros[indice]);
}